# PlatformIO
# Compiled output and environments
.pio/
.pioenvs/

# Dependency libraries (if not managed via platformio.ini's lib_deps)
.piolibdeps/

# VS Code files (optional, if you want to ignore user-specific workspace settings)
.vscode/
# except for the extensions.json and settings.json if you want to share recommendations and project settings
!.vscode/extensions.json
!.vscode/settings.json

# Other common files to ignore
*.pyc
*.elc
*.o
*.a

# Log files and crash reports
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.rpt2_cache/
.eslintcache
.stylelintcache
.node_repl_history

# OS and editor files
.DS_Store
.venv/
.idea/
